# 项目结构规范

## 页面结构

- 页面按业务模块划分
- 禁止所有页面集中在同一目录
- 禁止跨模块引用

## 组件结构

- 公共组件必须与业务无关
- 页面组件禁止进入全局组件库
- 模块组件仅服务当前模块

## API 层

- 所有请求必须统一封装
- 禁止组件内直接调用请求库
- 禁止拼接请求逻辑

## 类型管理

- 公共类型与模块类型分离
- 禁止重复定义类型
- 禁止复杂 inline 类型
