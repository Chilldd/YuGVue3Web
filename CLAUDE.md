# 后端接口

查询后端接口定义时，使用python解析http://localhost:5000/swagger/v1/swagger.json，从中获取接口定义

# 技术栈约束

统一使用：

- Vue 3（Composition API）
- TypeScript
- Vite
- Pinia
- Naive UI
- Axios
- <script setup>

禁止：

- Options API
- mixins
- JavaScript 文件
- jQuery

---

# 架构原则（最高优先级）

必须遵循：

- 高内聚低耦合
- UI 与业务逻辑分离
- API 与页面解耦
- 强制复用（组件 / composable / 类型）
- 避免重复实现
- 可维护性优先

---

# 项目结构规范

## 页面结构

- 页面按业务模块划分
- 禁止所有页面集中在同一目录
- 禁止跨模块引用

## 组件结构

- 公共组件必须与业务无关
- 页面组件禁止进入全局组件库
- 模块组件仅服务当前模块

## API 层

- 所有请求必须统一封装
- 禁止组件内直接调用请求库
- 禁止拼接请求逻辑

## 类型管理

- 公共类型与模块类型分离
- 禁止重复定义类型
- 禁止复杂 inline 类型

---

# Vue 开发规范

必须使用 Composition API

禁止：

- Options API
- mixins
- 模板中复杂业务逻辑

组件要求：

- 单一职责
- 可复用
- 复杂组件必须拆分
- 业务逻辑必须抽离 composable

---

# Composable 规范

用于：

- 可复用业务逻辑
- 非 UI 逻辑封装

要求：

- 不得耦合 UI
- 必须可复用
- 必须可测试
- 页面专属逻辑禁止进入公共 composable

---

# TypeScript 规范

强制要求：

- 必须完整类型定义
- 禁止 any
- 禁止 ts-ignore
- 禁止滥用 unknown

API 返回必须统一结构化管理

---

# 状态管理（Pinia）

Pinia 仅用于：

- 用户信息
- 权限信息
- 全局配置
- 跨页面状态
- 可缓存业务数据

禁止：

- 表单状态
- 弹窗状态
- 页面局部状态

---

# 权限规范

必须统一管理权限逻辑

禁止：

- 页面硬编码权限判断
- 模板中大量 v-if 权限判断

---

# 表格页面规范

必须具备：

- loading 状态
- 空状态处理
- 分页
- 错误处理
- 请求失败重试

---

# 表单规范

必须具备：

- 完整校验机制
- 防重复提交
- 提交期间锁定操作

禁止：

- 校验逻辑分散
- 重复校验实现

---

# 样式规范

必须遵循：

- scoped style
- CSS 变量体系
- BEM 命名规范

禁止：

- 内联样式
- 全局污染
- 深层嵌套滥用

---

# 性能规范

禁止：

- 深 watch 滥用
- 大对象 reactive
- 重复 API 请求
- 页面初始化过多请求

推荐：

- computed 优先
- shallowRef
- 懒加载
- 路由拆分

---

# 命名规范

- 组件：大驼峰（PascalCase）
- 变量：小驼峰（camelCase）
- 常量：全大写下划线

---

# 日志规范

生产环境禁止：

- console.log
- debugger

---

# AI 生成代码规范（关键）

AI 必须遵守：

## 优先级原则

1. 优先复用已有实现
2. 优先复用组件
3. 优先复用 composable
4. 优先复用类型
5. 保持现有架构一致

## 修改原则

- 不修改无关代码
- 最小变更范围
- 不破坏结构

## 不确定时

- 必须保持与现有代码风格一致

---

# 严格禁止

- 组件内直接请求接口
- 使用 any 类型
- 超大单文件组件
- 多职责组件
- 模板复杂逻辑
- 魔法数字
- 重复 API 调用
- 直接修改 props
- 无类型 API
- 无确认框的删除/启用/禁用操作

---

# 提交前必须检查

必须确认：

- 类型完整
- 无 lint 问题
- 无冗余 import
- 无重复逻辑
- 命名一致
- 组件职责单一
- API 类型完整
- composable 可复用